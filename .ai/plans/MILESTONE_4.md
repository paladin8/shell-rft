# Milestone 4: Prompt-Only Baseline Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Evaluate the raw qwen3-8b model on held-out test data to establish a prompt-only baseline before RFT training.

**Architecture:** The baseline script loads test examples from JSONL, sends each example's messages to the Fireworks API, then runs the model's response through the evaluator's existing extraction/validation/sandbox/normalization pipeline. It tracks detailed per-example results and computes aggregate metrics. The evaluator code is imported via sys.path to keep the evaluator directory self-contained.

**Tech Stack:** Python, fireworks-ai SDK (OpenAI-compatible chat completions API), existing evaluator code from `evaluators/shell_rft_v0/`.

---

## Design Decisions

### Evaluator reuse strategy

The evaluator in `evaluators/shell_rft_v0/` is self-contained (Fireworks Build SDK requirement). Rather than duplicating its logic, the baseline script adds `evaluators/` to `sys.path` (the parent directory, so that `shell_rft_v0` is importable as a package) and imports the sub-functions directly. This gives us detailed per-stage failure tracking without duplicating code.

### API call approach

Sequential calls, not async. The test set is 50 examples with short prompts and short responses — total runtime is dominated by API latency but still under a few minutes. No need for concurrency complexity.

### Qwen3 reasoning mode

Qwen3 models default to reasoning-on behavior (chain-of-thought before the answer). For the baseline, we want the model's raw command output without reasoning tokens inflating the response. The Fireworks API supports a `reasoning_effort` parameter — we'll use `"off"` to disable reasoning. If this parameter isn't available for the model, we fall back to a short `max_tokens` limit.

### Decoding parameters

Temperature 0 (greedy) for a deterministic, reproducible baseline. This gives us a single "best guess" per example rather than sampling variance.

### Results format

Results are saved to `data/baseline_results.json` with per-example detail and aggregate metrics. The script also prints a human-readable summary table to stdout.

---

## File Structure

```
scripts/
  run_local_baseline.py   # MODIFY — replace stub with full implementation
data/
  train.jsonl             # GENERATE — 200 examples (50 per family)
  val.jsonl               # GENERATE — 50 examples
  test.jsonl              # GENERATE — 50 examples
  baseline_results.json   # OUTPUT — generated by baseline script
```

No new library code. No new test files — this is a CLI script that calls an external API, so its correctness is verified by running it and inspecting results.

---

## Task 1: Generate Datasets

**Files:**
- Generate: `data/train.jsonl`, `data/val.jsonl`, `data/test.jsonl`

- [ ] **Step 1: Run the dataset generation script**

```bash
cd /home/jeffr/code/shell-rft && uv run python scripts/generate_dataset.py
```

Expected output:
```
Wrote 200 examples to data/train.jsonl
Wrote 50 examples to data/val.jsonl
Wrote 50 examples to data/test.jsonl
```

- [ ] **Step 2: Verify dataset contents**

```bash
cd /home/jeffr/code/shell-rft && wc -l data/*.jsonl
```

Expected: 200 lines in train, 50 in val, 50 in test.

Also verify task type distribution:

```bash
cd /home/jeffr/code/shell-rft && python3 -c "
import json
from collections import Counter
for split in ['train', 'val', 'test']:
    counts = Counter()
    with open(f'data/{split}.jsonl') as f:
        for line in f:
            counts[json.loads(line)['task_type']] += 1
    print(f'{split}: {dict(sorted(counts.items()))}')
"
```

Expected: roughly equal distribution across all 4 task families in each split.

- [ ] **Step 3: Spot-check a few examples**

```bash
cd /home/jeffr/code/shell-rft && head -1 data/test.jsonl | python3 -m json.tool | head -20
```

Verify the example has: `messages` (2 entries), `workspace_spec` (with files), `expected_stdout` (non-empty), `task_type` (one of the 4 families).

---

## Task 2: Implement Baseline Script

**Files:**
- Modify: `scripts/run_local_baseline.py` (replace stub)

- [ ] **Step 1: Write the complete baseline script**

Replace the contents of `scripts/run_local_baseline.py` with:

```python
"""Run prompt-only baseline evaluation on held-out data.

Sends each test example to the Fireworks API, evaluates the model's response
using the evaluator pipeline, and reports aggregate metrics.

Requires FIREWORKS_API_KEY environment variable.
"""

import argparse
import json
import os
import sys
from pathlib import Path

# Add evaluators/ to sys.path so we can import shell_rft_v0 as a package.
_EVAL_DIR = str(Path(__file__).resolve().parent.parent / "evaluators")
sys.path.insert(0, _EVAL_DIR)

from shell_rft_v0.normalize import normalize_stdout  # noqa: E402
from shell_rft_v0.policy import extract_single_command, validate_command  # noqa: E402
from shell_rft_v0.sandbox import materialize_workspace, run_command  # noqa: E402

import shutil  # noqa: E402
import tempfile  # noqa: E402

from fireworks.client import Fireworks  # noqa: E402


def load_examples(path: str) -> list[dict]:
    """Load JSONL examples from a file."""
    examples = []
    with open(path) as f:
        for line in f:
            examples.append(json.loads(line))
    return examples


def call_model(
    client: Fireworks,
    model: str,
    messages: list[dict],
    temperature: float = 0.0,
    max_tokens: int = 256,
) -> str:
    """Call the Fireworks API and return the assistant's response text."""
    response = client.chat.completions.create(
        model=model,
        messages=messages,
        temperature=temperature,
        max_tokens=max_tokens,
    )
    return response.choices[0].message.content or ""


def evaluate_response(
    response_text: str,
    workspace_spec: dict,
    expected_stdout: str,
    task_type: str,
) -> dict:
    """Evaluate a model response through the evaluator pipeline.

    Returns a dict with stage-by-stage results:
      - command: extracted command or None
      - parse_ok: bool
      - policy_ok: bool (None if parse failed)
      - policy_reason: str or None
      - exec_ok: bool (None if policy failed)
      - exit_code: int or None
      - actual_stdout: str or None
      - match: bool (None if exec failed)
      - score: float (1.0 or 0.0)
    """
    result = {
        "response_text": response_text,
        "command": None,
        "parse_ok": False,
        "policy_ok": None,
        "policy_reason": None,
        "exec_ok": None,
        "exit_code": None,
        "actual_stdout": None,
        "match": None,
        "score": 0.0,
    }

    # Stage 1: Extract command.
    command = extract_single_command(response_text)
    if command is None:
        return result
    result["command"] = command
    result["parse_ok"] = True

    # Stage 2: Validate policy.
    violation = validate_command(command)
    if violation is not None:
        result["policy_ok"] = False
        result["policy_reason"] = violation
        return result
    result["policy_ok"] = True

    # Stage 3: Execute in sandbox.
    workdir = tempfile.mkdtemp(prefix="shell_rft_baseline_")
    try:
        materialize_workspace(workspace_spec, workdir)
        exit_code, stdout, stderr = run_command(command, workdir)
    finally:
        shutil.rmtree(workdir, ignore_errors=True)

    result["exit_code"] = exit_code
    if exit_code != 0:
        result["exec_ok"] = False
        return result
    result["exec_ok"] = True
    result["actual_stdout"] = stdout

    # Stage 4: Compare output.
    actual = normalize_stdout(stdout, task_type=task_type)
    expected = normalize_stdout(expected_stdout, task_type=task_type)
    result["match"] = actual == expected
    result["score"] = 1.0 if result["match"] else 0.0

    return result


def compute_metrics(results: list[dict]) -> dict:
    """Compute aggregate metrics from per-example results."""
    n = len(results)
    if n == 0:
        return {}

    metrics = {
        "total": n,
        "exact_match": sum(1 for r in results if r["score"] == 1.0),
        "parse_success": sum(1 for r in results if r["parse_ok"]),
        "policy_pass": sum(1 for r in results if r["policy_ok"] is True),
        "policy_fail": sum(1 for r in results if r["policy_ok"] is False),
        "exec_success": sum(1 for r in results if r["exec_ok"] is True),
        "exec_fail": sum(1 for r in results if r["exec_ok"] is False),
        "output_match": sum(1 for r in results if r["match"] is True),
        "output_mismatch": sum(1 for r in results if r["match"] is False),
    }

    metrics["exact_match_rate"] = metrics["exact_match"] / n
    metrics["parse_success_rate"] = metrics["parse_success"] / n
    metrics["mean_reward"] = sum(r["score"] for r in results) / n

    # Per-task-family breakdown.
    by_family: dict[str, list[dict]] = {}
    for r in results:
        family = r.get("task_type", "unknown")
        by_family.setdefault(family, []).append(r)

    family_metrics = {}
    for family, family_results in sorted(by_family.items()):
        fn = len(family_results)
        em = sum(1 for r in family_results if r["score"] == 1.0)
        family_metrics[family] = {
            "total": fn,
            "exact_match": em,
            "exact_match_rate": em / fn,
        }
    metrics["by_family"] = family_metrics

    return metrics


def print_summary(metrics: dict) -> None:
    """Print a human-readable summary table."""
    n = metrics["total"]
    print("\n" + "=" * 60)
    print("BASELINE RESULTS")
    print("=" * 60)
    print(f"  Total examples:      {n}")
    print(f"  Exact match:         {metrics['exact_match']}/{n} ({metrics['exact_match_rate']:.1%})")
    print(f"  Mean reward:         {metrics['mean_reward']:.3f}")
    print(f"  Parse success:       {metrics['parse_success']}/{n} ({metrics['parse_success_rate']:.1%})")
    print(f"  Policy pass:         {metrics['policy_pass']}/{n}")
    print(f"  Policy fail:         {metrics['policy_fail']}/{n}")
    print(f"  Exec success:        {metrics['exec_success']}/{n}")
    print(f"  Exec fail:           {metrics['exec_fail']}/{n}")
    print(f"  Output match:        {metrics['output_match']}/{n}")
    print(f"  Output mismatch:     {metrics['output_mismatch']}/{n}")
    print()
    print("Per task family:")
    print(f"  {'Family':<20} {'Match':>6} {'Total':>6} {'Rate':>8}")
    print(f"  {'-'*20} {'-'*6} {'-'*6} {'-'*8}")
    for family, fm in metrics["by_family"].items():
        print(f"  {family:<20} {fm['exact_match']:>6} {fm['total']:>6} {fm['exact_match_rate']:>8.1%}")
    print("=" * 60)


def main() -> None:
    parser = argparse.ArgumentParser(
        description="Run prompt-only baseline evaluation on held-out data."
    )
    parser.add_argument(
        "--model",
        type=str,
        default="accounts/fireworks/models/qwen3-8b",
        help="Fireworks model identifier",
    )
    parser.add_argument(
        "--dataset",
        type=str,
        default="data/test.jsonl",
        help="Path to JSONL dataset to evaluate",
    )
    parser.add_argument(
        "--output",
        type=str,
        default="data/baseline_results.json",
        help="Path to save detailed results JSON",
    )
    parser.add_argument(
        "--max-tokens",
        type=int,
        default=256,
        help="Max tokens for model response",
    )
    parser.add_argument(
        "--temperature",
        type=float,
        default=0.0,
        help="Sampling temperature (0 = greedy)",
    )
    parser.add_argument(
        "--limit",
        type=int,
        default=0,
        help="Limit number of examples to evaluate (0 = all)",
    )
    args = parser.parse_args()

    api_key = os.environ.get("FIREWORKS_API_KEY")
    if not api_key:
        print("Error: FIREWORKS_API_KEY environment variable is required.")
        sys.exit(1)

    # Load examples.
    examples = load_examples(args.dataset)
    if args.limit > 0:
        examples = examples[: args.limit]
    print(f"Loaded {len(examples)} examples from {args.dataset}")

    # Initialize Fireworks client.
    client = Fireworks(api_key=api_key)

    # Evaluate each example.
    results = []
    for i, ex in enumerate(examples):
        task_type = ex.get("task_type", "unknown")
        messages = ex["messages"]

        print(f"  [{i+1}/{len(examples)}] {task_type}...", end=" ", flush=True)

        try:
            response_text = call_model(
                client,
                args.model,
                messages,
                temperature=args.temperature,
                max_tokens=args.max_tokens,
            )
        except Exception as e:
            print(f"API error: {e}")
            result = {
                "response_text": "",
                "command": None,
                "parse_ok": False,
                "policy_ok": None,
                "policy_reason": None,
                "exec_ok": None,
                "exit_code": None,
                "actual_stdout": None,
                "match": None,
                "score": 0.0,
                "error": str(e),
            }
            result["task_type"] = task_type
            results.append(result)
            continue

        result = evaluate_response(
            response_text,
            ex["workspace_spec"],
            ex["expected_stdout"],
            task_type,
        )
        result["task_type"] = task_type

        status = "MATCH" if result["score"] == 1.0 else "MISS"
        print(f"{status} (cmd: {result['command']})")

        results.append(result)

    # Compute and display metrics.
    metrics = compute_metrics(results)
    print_summary(metrics)

    # Save detailed results.
    output_data = {
        "model": args.model,
        "dataset": args.dataset,
        "temperature": args.temperature,
        "max_tokens": args.max_tokens,
        "metrics": metrics,
        "results": results,
    }

    output_path = Path(args.output)
    output_path.parent.mkdir(parents=True, exist_ok=True)
    with open(output_path, "w") as f:
        json.dump(output_data, f, indent=2)
    print(f"\nDetailed results saved to {output_path}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Verify the script parses without errors**

```bash
cd /home/jeffr/code/shell-rft && uv run python -c "import ast; ast.parse(open('scripts/run_local_baseline.py').read()); print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Verify imports work**

```bash
cd /home/jeffr/code/shell-rft && uv run python -c "
import sys
from pathlib import Path
sys.path.insert(0, str(Path('evaluators').resolve()))
from shell_rft_v0.normalize import normalize_stdout
from shell_rft_v0.policy import extract_single_command, validate_command
from shell_rft_v0.sandbox import materialize_workspace, run_command
print('All evaluator imports OK')
"
```

Expected: `All evaluator imports OK`

- [ ] **Step 4: Verify the script shows help without API key**

```bash
cd /home/jeffr/code/shell-rft && uv run python scripts/run_local_baseline.py --help
```

Expected: help text showing all CLI arguments.

- [ ] **Step 5: Verify the script exits cleanly when no API key is set**

```bash
cd /home/jeffr/code/shell-rft && unset FIREWORKS_API_KEY && uv run python scripts/run_local_baseline.py 2>&1; echo "exit: $?"
```

Expected: error message about missing API key, exit code 1.

- [ ] **Step 6: Run existing tests to confirm nothing is broken**

```bash
cd /home/jeffr/code/shell-rft && uv run pytest -q
```

Expected: all 36 tests pass.

---

## Task 3: Run Baseline (requires API key)

This task requires a valid `FIREWORKS_API_KEY` environment variable. It calls the Fireworks API and costs real money (minimal — ~50 short prompts to qwen3-8b).

**Files:**
- Generate: `data/baseline_results.json`

- [ ] **Step 1: Do a dry run with --limit 2**

```bash
cd /home/jeffr/code/shell-rft && uv run python scripts/run_local_baseline.py --limit 2
```

Expected: 2 examples evaluated, summary printed, results saved. Verify the output looks reasonable — the model may or may not get these right.

If the API call fails, debug based on the error:
- 401: check API key
- 404: check model name (try `accounts/fireworks/models/qwen3-8b`)
- If the model emits reasoning tokens mixed with the command, consider adding `--max-tokens 128` or investigating the `reasoning_effort` parameter

- [ ] **Step 2: Inspect the dry-run results file**

```bash
cd /home/jeffr/code/shell-rft && python3 -c "
import json
with open('data/baseline_results.json') as f:
    data = json.load(f)
print('Model:', data['model'])
print('Results count:', len(data['results']))
for r in data['results']:
    print(f\"  task={r['task_type']} parse={r['parse_ok']} policy={r['policy_ok']} exec={r['exec_ok']} match={r['match']} score={r['score']}\")
    if r.get('command'):
        print(f\"    cmd: {r['command']}\")
    if r.get('response_text'):
        print(f\"    response: {r['response_text'][:200]}\")
"
```

Review the output carefully. Check:
- Is the model returning a single command or extra text?
- Are commands reasonable for the task type?
- If parse_ok is False, what does the response look like?

- [ ] **Step 3: Run the full baseline**

```bash
cd /home/jeffr/code/shell-rft && uv run python scripts/run_local_baseline.py
```

Expected: 50 examples evaluated with summary table printed. Save the terminal output for the analysis memo (milestone 6).

- [ ] **Step 4: Review results and note observations**

After the full run, examine:
1. Overall exact-match rate — if >90%, the task may be too easy
2. Per-family breakdown — which families are hardest?
3. Failure modes — are failures mostly parse issues (reasoning tokens), policy violations, execution errors, or output mismatches?
4. Sample of mismatches — look at a few output_mismatch cases to understand if the model is close or completely wrong

```bash
cd /home/jeffr/code/shell-rft && python3 -c "
import json
with open('data/baseline_results.json') as f:
    data = json.load(f)

# Show mismatches
mismatches = [r for r in data['results'] if r['match'] is False]
print(f'Output mismatches: {len(mismatches)}')
for r in mismatches[:5]:
    print(f\"  task_type: {r['task_type']}\")
    print(f\"  command:   {r['command']}\")
    print(f\"  actual:    {repr(r['actual_stdout'][:100])}\")
    print()

# Show parse failures
parse_fails = [r for r in data['results'] if not r['parse_ok']]
print(f'Parse failures: {len(parse_fails)}')
for r in parse_fails[:3]:
    print(f\"  response: {repr(r['response_text'][:200])}\")
    print()
"
```

---

## Notes

### What this milestone does NOT include

- **Baseline B (constrained output):** The design doc mentions a grammar-constrained baseline. That's a separate experiment — not needed for milestone 4. We can add it later if the parse-failure rate is high enough to warrant it.
- **Automated tests for the script:** This is a CLI tool that calls an external API. Its correctness is verified by running it and inspecting results, not by mocking the API.
- **Retry logic:** If an API call fails, the script logs the error and moves on. For 50 examples, manual re-runs are easier than retry machinery.

### Interpreting results

The baseline results inform milestone 5 (first RFT run):
- If exact-match is very low (<20%), the task format may need adjustment
- If exact-match is very high (>80%), the tasks are too easy and we need harder examples
- If most failures are parse issues (reasoning tokens), we should investigate the `reasoning_effort` API parameter or add response post-processing
- Per-family differences tell us which families will benefit most from RFT

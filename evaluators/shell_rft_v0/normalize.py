"""Stdout normalization for comparison."""


def normalize_stdout(stdout: str, task_type: str | None = None) -> str:
    """Normalize stdout for comparison. Strips trailing whitespace, etc."""
    raise NotImplementedError
